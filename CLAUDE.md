# Dependencies

The project's Lua dependencies have been installed via `luarocks install --local --only-deps`, but that's a detail of the environment and shouldn't make its way into this codebase.

# Tests

When adding features or fixing bugs, make sure you add tests to guard against regressions in the future.
